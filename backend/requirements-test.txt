# backend/requirements-test.txt
# Pins only the lightweight packages needed for deterministic backend tests.
# Tests inject a fake vector store and never download or invoke an embedding model.
fastapi==0.115.12
httpx==0.28.1
pytest==8.3.5
