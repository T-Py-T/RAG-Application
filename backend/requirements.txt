# backend/requirements.txt
# Pins the packages required to run the local semantic-retrieval example.
# It intentionally excludes cloud SDKs and hosted-model clients.
fastapi==0.115.12
faiss-cpu==1.11.0
langchain==0.3.25
langchain-community==0.3.24
sentence-transformers==4.1.0
uvicorn==0.34.2
