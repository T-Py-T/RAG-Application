# frontend/pnpm-workspace.yaml
# Allows native build scripts already locked for the local Next.js toolchain.
# It does not add packages, workspaces, or runtime services.
autoInstallPeers: false
allowBuilds:
  '@tailwindcss/oxide': true
  esbuild: true
  sharp: true
